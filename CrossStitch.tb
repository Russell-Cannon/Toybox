n = 32
stitch(y1, y2) = smoothstep(0.45, 0.55, linesegment(fract(uv*n), combine(.25, y1, 0), combine(.75, y2, 0)))
crossPattern = stitch(.25, .75) * stitch(.75, .25)

(texture(floor(uv * n) / n) * (1 - crossPattern)) + crossPattern